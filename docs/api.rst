API Reference
=============

All endpoints return ``application/json`` unless noted. During local
development the base URL is ``http://localhost:5000``.

Endpoint summary
----------------

.. qrefflask:: app:create_app()
   :undoc-static:

Endpoints
---------

.. autoflask:: app:create_app()
   :undoc-static:
   :order: path
