Internals
=========

The Python building blocks behind the API: the application factory and the
per-environment configuration classes. These are documented with
``autodoc`` because they *are* a Python API (unlike the HTTP routes, which live
on the :doc:`api` page).

Application factory
-------------------

.. automodule:: app
   :members:
   :undoc-members:
   :show-inheritance:

Configuration
-------------

.. automodule:: app.config
   :members:
   :undoc-members:
   :show-inheritance:
